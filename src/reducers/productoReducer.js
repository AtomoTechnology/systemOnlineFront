// import { types } from "../types/types";

const initialState = {
	producto: 123,
};

export const productoReducer = (state = initialState, action) => {
	switch (action.type) {
		default:
			return state;
	}
};
